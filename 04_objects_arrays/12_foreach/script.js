//'forEach()'

//O método 'forEach()' chama uma função para cada elemento do 'array', sendo executado apenas em elementos não vazios.

let partes = ["rodas", "motor", "tanque", "painel", "chicote"]
partes.forEach(partes => {
    console.log("Partes de " + partes)
})